import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.droply',
  appName: 'Droply',
  webDir: 'public',

  server: {
    url: 'https://droply.dhyeyparekh.in',
  },
};

export default config;
